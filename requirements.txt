# DysonianLineCNN — Python dependencies
#
# Version floors are placeholders during Phase 0 of the migration. Exact
# versions will be pinned in Phase 6 from a live Colab runtime so that the
# article's reproducibility artifacts match the environment we actually
# trained on.
#
# For Mac (Apple Silicon) local training, install `tensorflow-metal` in
# addition to `tensorflow` to enable GPU acceleration via Metal.

# --- Core ML ---
tensorflow>=2.15
numpy>=1.24
scipy>=1.11
scikit-learn>=1.3
pandas>=2.0
matplotlib>=3.7

# --- Testing ---
pytest>=7.4

# --- Notebook hygiene (git filter for output stripping) ---
nbstripout>=0.9
